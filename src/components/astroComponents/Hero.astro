
<div class="heroContainer">
        <span class="circle1"></span>
        <span class="circle2"></span>
        <span class="circle3"></span>
        <span class="circle4"></span>
    <div class="fade">
        <h1>Transformamos tu idea en un <span>sitio web </span> web que hará crecer tu negocio</h1>
        <span class="space"></span>
        <div class="buttons">
            <a href="#" class="descubrir">Descubrir</a>
            <a href="#" class="contactar">Contactar</a>
        </div>
    </div>
</div>


<style>
    .heroContainer{
        height: auto;
        max-width: 100vw;
    }

    .fade{
        height: 120vh;
        max-width: 100vw;
        padding-top: 13%;
        display: flex;
        flex-direction: column;
        justify-content: start;
        align-items: center;
        z-index: 2;
        backdrop-filter: blur(20px);
        overflow-y: hidden;
    }

    .fade > h1{
        color: var(--lightWhite);
        font-size: 4.2rem;
        width: 70%;
        text-align: center;
        letter-spacing: 0.3rem;

    }

    .fade > h1 > span{
        color: var(--skyBlue);
        z-index: 2;
    }

    .buttons{
        margin-top: 4rem;
        height: auto;
        width: auto;
        
    }

    .buttons > a{
        font-size: 1.6rem;
        font-weight: bold;
        text-decoration: none;
        padding: 0.4rem 2.8rem;
        border-radius: 6px;
        transition: box-shadow ease-in-out 0.2s;
    }

    .descubrir{
        margin-right: 3rem;
        background-color: var(--skyBlue);
        color: var(--darkBlue);
    }

    .contactar{
        outline: solid 3px var(--skyBlue);
        color: var(--skyBlue);
    }

    .descubrir:hover{
        box-shadow: 0px 0px 14px 2px rgba(92, 225, 230, 0.6);
    }

    .contactar:hover{
        box-shadow: 0px 0px 14px 2px rgba(92, 225, 230, 0.6);
    }


    /* background aniamtions elements */

    @keyframes light {
        0% { opacity: 0.3;}
        25% { opacity: :0.1;}
        50% { opacity: 0.1;}
        75% { opacity: 0.3;}
        100% { opacity: 0.1;}
    }

    .circle1{
        height: 600px;
        width: 500px;
        background-image: radial-gradient(circle, rgba(92, 225, 230, 0.6), rgba(92, 225, 230, 0));
        border-radius: 50%;
        z-index: -1;
        position: absolute;
        left: -200px;
        top: -180px;
        opacity: 0.3;
        animation: light 5s ease-in-out infinite alternate-reverse;
    }

    .circle2{
        height: 350px;
        width: 350px;
        background-image: radial-gradient(circle, rgba(92, 225, 230, 0.6), rgba(92, 225, 230, 0));
        border-radius: 50%;
        z-index: -2;
        position: absolute;
        bottom: -100px;
        left: 200px;
        opacity: 0.3;
        animation: light 6s ease-in-out infinite alternate-reverse;
    }

    .circle3{
        height: 350px;
        width: 450px;
        background-image: radial-gradient(circle, rgba(92, 225, 230, 0.6), rgba(92, 225, 230, 0));
        border-radius: 50%;
        z-index: -3;
        position: absolute;
        top: -100px;
        right: -100px;
        opacity: 0.3;
        animation: light 5s ease-in-out infinite alternate-reverse;
    }

    .circle4{
        height: 350px;
        width: 350px;
        background-image: radial-gradient(circle, rgba(92, 225, 230, 0.6), rgba(92, 225, 230, 0));
        border-radius: 50%;
        z-index: -3;
        position: absolute;
        bottom: -120px;
        right: 0px;
        opacity: 0.3;
        animation: light 6s ease-in-out infinite alternate-reverse;
    }
    
</style>